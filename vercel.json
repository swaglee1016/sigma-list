{
  "installCommand": "npm install",
  "buildCommand": "npx vite build && mkdir -p dist/api && cp api/sync.js dist/api/sync.js",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/$1" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
