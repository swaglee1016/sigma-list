{
  "installCommand": "npm install",
  "buildCommand": "npx vite build && mkdir -p dist/api && cp api/sync.cjs dist/api/sync.cjs",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ]
}
