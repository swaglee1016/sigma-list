{
  "installCommand": "npm install",
  "buildCommand": "npx vite build && mkdir -p dist/api && cp api/sync.mjs dist/api/",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}