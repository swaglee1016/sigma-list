{
  "buildCommand": "npx vite build && mkdir -p dist/api && cp api/sync.mjs dist/api/sync.mjs",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/$1" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
